[package]
name = "overwatch-data"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Embeds the committed hero/map/matchup TOML and resolves it into an overwatch-core Dataset."

[dependencies]
overwatch-core = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
